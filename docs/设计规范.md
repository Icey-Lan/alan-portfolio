# ALan Portfolio - 设计规范文档

**版本**：V1.0  
**日期**：2026-02-04  
**风格**：梦幻渐变 + 毛玻璃 (Dreamy Gradient + Glassmorphism)

---

## 1. 设计理念

### 1.1 设计关键词

| 关键词 | 描述 |
|-------|------|
| **Dreamy（梦幻）** | 柔和的色彩过渡，营造治愈、放松的视觉氛围 |
| **Glassmorphism（毛玻璃）** | 半透明模糊效果，层次分明，现代感十足 |
| **Kawaii（治愈系）** | 源自头像的可爱风格，星星、云朵等装饰元素 |
| **Motion（动态）** | 流畅的动画效果，增强交互体验 |

### 1.2 设计灵感来源

| 参考 | 借鉴元素 |
|-----|---------|
| [xmmxovo.online](https://xmmxovo.online/) | 布局结构、胶囊导航、项目详情弹窗 |
| [reflect.app](https://reflect.app/) | 深色背景、发光效果、毛玻璃卡片 |
| [stripe.com](https://stripe.com/) | 流动渐变背景、色彩过渡 |
| 用户头像 | 天蓝 + 粉色配色灵感 |

---

## 2. 配色系统

### 2.1 核心调色板

| 角色 | 颜色名称 | HEX | RGB | 用途 |
|-----|---------|-----|-----|------|
| **Primary** | 天蓝 | `#6EC5FF` | rgb(110, 197, 255) | 主强调色、链接、按钮 |
| **Secondary** | 粉紫 | `#FFB3D9` | rgb(255, 179, 217) | 次强调色、装饰 |
| **Accent** | 淡紫 | `#C7B8EA` | rgb(199, 184, 234) | 辅助装饰、hover 状态 |
| **Background Light** | 浅灰 | `#F8FAFC` | rgb(248, 250, 252) | 浅色模式背景 |
| **Background Dark** | 深蓝紫 | `#1A1A2E` | rgb(26, 26, 46) | 深色模式背景 |
| **Text Primary** | 深灰 | `#0F172A` | rgb(15, 23, 42) | 主要文字 |
| **Text Muted** | 中灰 | `#64748B` | rgb(100, 116, 139) | 次要文字 |

### 2.2 渐变色

```css
/* 主背景渐变 */
--gradient-bg: linear-gradient(
  135deg, 
  #1A1A2E 0%, 
  #2D2A4A 50%, 
  #1A1A2E 100%
);

/* Aurora 渐变（装饰用） */
--gradient-aurora: radial-gradient(
  ellipse at 20% 20%, 
  rgba(110, 197, 255, 0.3) 0%, 
  transparent 50%
), radial-gradient(
  ellipse at 80% 80%, 
  rgba(255, 179, 217, 0.3) 0%, 
  transparent 50%
);

/* 按钮渐变 */
--gradient-cta: linear-gradient(
  135deg, 
  #6EC5FF 0%, 
  #C7B8EA 100%
);
```

### 2.3 毛玻璃效果参数

```css
/* 卡片毛玻璃 */
.glass-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 16px;
}

/* 导航栏毛玻璃 */
.glass-nav {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.15);
}

/* 浅色模式毛玻璃 */
.glass-card-light {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(0, 0, 0, 0.1);
}
```

---

## 3. 字体系统

### 3.1 字体选择

| 用途 | 字体族 | 备选 | 字重 |
|-----|-------|------|------|
| **中文标题** | Noto Sans SC | system-ui | 700 (Bold) |
| **英文标题** | Outfit | Inter, SF Pro | 600-700 |
| **正文** | Noto Sans SC | system-ui | 400-500 |
| **手写风格** | Caveat | Ma Shan Zheng | 400-600 |
| **代码** | JetBrains Mono | Fira Code | 400 |

### 3.2 Google Fonts 引入

```css
@import url('https://fonts.googleapis.com/css2?family=Caveat:wght@400;500;600;700&family=Noto+Sans+SC:wght@300;400;500;600;700&family=Outfit:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap');
```

### 3.3 字体层级

| 元素 | 字号 (Desktop) | 字号 (Mobile) | 行高 | 字重 |
|-----|---------------|--------------|------|------|
| H1 | 64px / 4rem | 40px / 2.5rem | 1.1 | 700 |
| H2 | 48px / 3rem | 32px / 2rem | 1.2 | 600 |
| H3 | 32px / 2rem | 24px / 1.5rem | 1.3 | 600 |
| H4 | 24px / 1.5rem | 20px / 1.25rem | 1.4 | 500 |
| Body | 16px / 1rem | 16px / 1rem | 1.6 | 400 |
| Small | 14px / 0.875rem | 14px / 0.875rem | 1.5 | 400 |
| Caption | 12px / 0.75rem | 12px / 0.75rem | 1.4 | 400 |

### 3.4 Tailwind 配置

```js
// tailwind.config.ts
fontFamily: {
  sans: ['Noto Sans SC', 'Outfit', 'system-ui', 'sans-serif'],
  display: ['Outfit', 'Noto Sans SC', 'sans-serif'],
  handwritten: ['Caveat', 'Ma Shan Zheng', 'cursive'],
  mono: ['JetBrains Mono', 'Fira Code', 'monospace'],
}
```

---

## 4. 间距系统

### 4.1 基础间距

基于 8px 网格系统：

| Token | 值 | 用途 |
|-------|-----|------|
| `space-1` | 4px | 内联元素间距 |
| `space-2` | 8px | 紧凑元素间距 |
| `space-3` | 12px | 小间距 |
| `space-4` | 16px | 标准元素间距 |
| `space-6` | 24px | 卡片内边距 |
| `space-8` | 32px | 区块间距 |
| `space-12` | 48px | 大区块间距 |
| `space-16` | 64px | 区域分隔 |
| `space-24` | 96px | 页面区域间距 |

### 4.2 布局容器

```css
/* 最大宽度 */
--max-width-content: 1200px;
--max-width-narrow: 800px;
--max-width-wide: 1400px;

/* 页面边距 */
--page-padding-mobile: 16px;
--page-padding-tablet: 32px;
--page-padding-desktop: 64px;
```

---

## 5. 圆角系统

| Token | 值 | 用途 |
|-------|-----|------|
| `rounded-sm` | 4px | 小按钮、标签 |
| `rounded-md` | 8px | 按钮、输入框 |
| `rounded-lg` | 12px | 小卡片 |
| `rounded-xl` | 16px | 标准卡片 |
| `rounded-2xl` | 24px | 大卡片、模态框 |
| `rounded-3xl` | 32px | 特大卡片 |
| `rounded-full` | 9999px | 胶囊按钮、头像 |

---

## 6. 阴影系统

### 6.1 标准阴影

```css
/* 阴影层级 */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1);
--shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
--shadow-xl: 0 20px 25px rgba(0, 0, 0, 0.15);
```

### 6.2 发光阴影

```css
/* 主色发光 */
--glow-primary: 0 0 20px rgba(110, 197, 255, 0.4);

/* 粉色发光 */
--glow-secondary: 0 0 20px rgba(255, 179, 217, 0.4);

/* 混合发光 */
--glow-mixed: 
  0 0 20px rgba(110, 197, 255, 0.3),
  0 0 40px rgba(255, 179, 217, 0.2);
```

---

## 7. 动画系统

### 7.1 过渡时间

| 类型 | 时长 | 缓动函数 | 用途 |
|-----|------|---------|------|
| 快速 | 150ms | ease-out | 按钮 hover、颜色变化 |
| 标准 | 200ms | ease-in-out | 卡片 hover、展开收起 |
| 慢速 | 300ms | ease-in-out | 模态框、页面过渡 |
| 弹性 | 400ms | cubic-bezier(0.34, 1.56, 0.64, 1) | 弹跳效果 |

### 7.2 入场动画

```css
/* 淡入上移 */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 缩放淡入 */
@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

/* 漂浮动画 */
@keyframes float {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10px);
  }
}
```

### 7.3 Framer Motion 预设

```tsx
// 入场动画变体
export const fadeInUp = {
  initial: { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  transition: { duration: 0.5, ease: "easeOut" }
};

// 卡片悬浮
export const cardHover = {
  whileHover: { 
    y: -8, 
    boxShadow: "0 20px 40px rgba(110, 197, 255, 0.2)" 
  },
  transition: { duration: 0.3 }
};

// 按钮点击
export const buttonTap = {
  whileTap: { scale: 0.95 },
  transition: { duration: 0.1 }
};

// 交错动画容器
export const staggerContainer = {
  initial: {},
  animate: {
    transition: {
      staggerChildren: 0.1
    }
  }
};
```

### 7.4 无障碍：减少动效

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 8. 组件规范

### 8.1 导航栏

```
┌────────────────────────────────────────────────────────────┐
│  [ Logo ]    首页  |  关于  |  项目  |  博客  |  联系      │
└────────────────────────────────────────────────────────────┘

样式：
- 胶囊形状 (rounded-full)
- 居中悬浮 (fixed top-6 left-1/2 -translate-x-1/2)
- 毛玻璃效果
- 高度：56px
- 内边距：px-8 py-3
- 导航项间距：space-x-8
```

### 8.2 按钮

| 变体 | 样式 |
|-----|------|
| **Primary** | 渐变背景 + 白色文字 + 发光阴影 |
| **Secondary** | 透明背景 + 边框 + 主色文字 |
| **Ghost** | 透明背景 + 主色文字 + hover 背景变化 |

```css
/* Primary Button */
.btn-primary {
  background: linear-gradient(135deg, #6EC5FF 0%, #C7B8EA 100%);
  color: white;
  padding: 12px 24px;
  border-radius: 9999px;
  font-weight: 500;
  box-shadow: 0 0 20px rgba(110, 197, 255, 0.3);
}

.btn-primary:hover {
  box-shadow: 0 0 30px rgba(110, 197, 255, 0.5);
  transform: translateY(-2px);
}
```

### 8.3 卡片

```css
.card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 24px;
  padding: 24px;
}

.card:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 40px rgba(110, 197, 255, 0.2);
}
```

### 8.4 标签

```css
.tag {
  display: inline-flex;
  align-items: center;
  padding: 6px 12px;
  background: rgba(110, 197, 255, 0.15);
  border: 1px solid rgba(110, 197, 255, 0.3);
  border-radius: 9999px;
  font-size: 14px;
  color: #6EC5FF;
}
```

---

## 9. 图标规范

### 9.1 推荐图标库

| 库 | 用途 | 风格 |
|---|------|------|
| **Lucide React** | 通用 UI 图标 | 线性、轻量 |
| **Heroicons** | 通用 UI 图标 | 实心/线性 |
| **Simple Icons** | 品牌 Logo | 官方标准 |

### 9.2 图标尺寸

| 场景 | 尺寸 |
|-----|------|
| 内联文本 | 16px (w-4 h-4) |
| 按钮图标 | 20px (w-5 h-5) |
| 卡片图标 | 24px (w-6 h-6) |
| 大图标 | 32px (w-8 h-8) |
| 特大图标 | 48px (w-12 h-12) |

### 9.3 禁止使用

❌ 不要使用 Emoji 作为 UI 图标（如 🎨 🚀 ⚙️）  
✅ 使用 SVG 图标库

---

## 10. 装饰元素

### 10.1 星星装饰

```tsx
// 四角星 SVG
<svg viewBox="0 0 24 24" fill="currentColor">
  <path d="M12 0L14.59 9.41L24 12L14.59 14.59L12 24L9.41 14.59L0 12L9.41 9.41L12 0Z" />
</svg>

// 使用方式：随机散布在背景中
// 大小：8px - 24px
// 透明度：0.3 - 0.8
// 动画：float（缓慢漂浮）
```

### 10.2 云朵装饰

```css
/* 云朵形状 */
.cloud {
  width: 100px;
  height: 40px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50px;
  position: relative;
}

.cloud::before {
  content: '';
  position: absolute;
  width: 50px;
  height: 50px;
  background: inherit;
  border-radius: 50%;
  top: -25px;
  left: 15px;
}
```

---

## 11. 响应式断点

| 断点 | 宽度 | 用途 |
|-----|------|------|
| `sm` | 640px | 横屏手机 |
| `md` | 768px | 平板竖屏 |
| `lg` | 1024px | 平板横屏/小笔记本 |
| `xl` | 1280px | 桌面 |
| `2xl` | 1536px | 大屏幕 |

### Tailwind 配置

```js
screens: {
  'sm': '640px',
  'md': '768px',
  'lg': '1024px',
  'xl': '1280px',
  '2xl': '1536px',
}
```

---

## 12. 交付前检查清单

### 视觉质量

- [ ] 未使用 Emoji 作为图标（使用 SVG 代替）
- [ ] 所有图标来自统一图标库 (Lucide/Heroicons)
- [ ] 品牌 Logo 正确（从 Simple Icons 验证）
- [ ] Hover 状态不造成布局偏移
- [ ] 直接使用主题色 (bg-primary) 而非 var() 包装

### 交互

- [ ] 所有可点击元素有 `cursor-pointer`
- [ ] Hover 状态有清晰视觉反馈
- [ ] 过渡动画流畅 (150-300ms)
- [ ] 键盘导航可见焦点状态

### 浅色/深色模式

- [ ] 浅色模式文字对比度充足 (4.5:1 最小)
- [ ] 毛玻璃/透明元素在浅色模式可见
- [ ] 边框在两种模式都可见
- [ ] 发布前测试两种模式

### 布局

- [ ] 悬浮元素与边缘有适当间距
- [ ] 内容不被固定导航栏遮挡
- [ ] 在 375px, 768px, 1024px, 1440px 响应正常
- [ ] 移动端无水平滚动

### 无障碍

- [ ] 所有图片有 alt 文本
- [ ] 表单输入有 label
- [ ] 颜色不是唯一指示器
- [ ] 尊重 `prefers-reduced-motion`

---

**文档结束**
